Шаблон lostpassword.entries.tpl
===============================

Данный шаблон отвечает за показ  стандартной переменной (наименование, описание, поле ввода) в шаблоне восстановления пароля.


Более детально про восстановление паролей можно прочитать в описании шаблона <a href="lostpassword.tpl.md"><b>lostpassword.tpl</b></a>.

Доступные блоки/переменные
==========================

Переменные:

<b>{title}</b> - Название переменной.
<b>{descr}</b> - Описание переменной.
<b>{input}</b> - Поле ввода для переменной.


Пример заполнения шаблона
=========================

В примере показан минимально набор для полнофункциональной работы:
<pre >{title} ({descr}): {input}</pre>
